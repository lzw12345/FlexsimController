package com.nusinfineon.ui;

import javafx.stage.Stage;

/**
 * API of user interface component.
 */
public interface Ui {

    /**
     * Starts the UI, which is equivalent to start the application.
     */
    void start(Stage primaryStage);

}
